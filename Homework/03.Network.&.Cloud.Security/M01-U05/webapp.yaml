apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-deployment
  labels:
    app: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: php:7.4-apache
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
        env:
        - name: USER_NAME
          valueFrom:
            secretKeyRef:
              name: mifiib-secret
              key: username
        - name: USER_PWD
          valueFrom:
            secretKeyRef:
              name: mifiib-secret
              key: password
        - name: DB_URL
          valueFrom:
            configMapKeyRef:
              name: mifiib-config
              key: mifiib-url
---

apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080