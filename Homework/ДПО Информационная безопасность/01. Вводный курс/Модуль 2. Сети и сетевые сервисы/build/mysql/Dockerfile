# Используется базовый образ MySQL версии 5.7
FROM mysql:5.7
# Устанавливает метку для контейнера
LABEL Yura Shamray
# Установка пользователя root внутри контейнера
USER root
# Установка разрешений доступа к директории /var/lib/mysql
RUN chmod 755 /var/lib/mysql
