FROM ubuntu:latest # Используем базовый образ Ubuntu
LABEL Yura Shamray # Устанавливаем метаданные для образа
RUN apt update && apt upgrade -y # Обновляем пакеты внутри образа
RUN apt install mysql-server -y # Устанавливаем пакет MySQL Server внутри образа
